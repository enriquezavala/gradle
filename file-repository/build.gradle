plugins{
    id 'java'
}

repositories {  // add local dependencies
    flatDir {
        dirs 'lib' // dependencies from this folder
    }
}

dependencies {
    implementation 'log4j:log4j:1.2.17' // group:name:version
}